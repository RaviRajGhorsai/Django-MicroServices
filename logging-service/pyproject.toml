[project]
name = "logging-service"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "RaviRajGhorsai", email = "graviraj344@gmail.com" }
]
requires-python = ">=3.13"
dependencies = [
    "djangorestframework>=3.18.0",
    "kafka-python>=3.0.11",
    "pymongo>=4.18.0",
]

[project.scripts]
logging-service = "logging_service:main"

[build-system]
requires = ["uv_build>=0.12.5,<0.13.0"]
build-backend = "uv_build"
